const { isJWT } = require("class-validator")



describe('user Reguister',()=>{

  beforeEach(()=>{
    cy.visit('http://localhost:3000')
  })
  it('user is able to register', ()0>{
   cy.containe
  })
})